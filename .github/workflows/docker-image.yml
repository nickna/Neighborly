name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Neighborly
      run: docker build . --file API.gRPC/Dockerfile --tag neighborly:$(date +%s)
